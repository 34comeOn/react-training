export type HistoryTyping= {
    searchDate: string,
    yearInStorage: string, 
    categoryInStorage: string
}

export type HistoryItemTyping = {
    searchDate: string,
    yearInStorage: string, 
    categoryInStorage: string,
    index: number,
}